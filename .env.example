# =============================================================================
# Photobook Configuration
# =============================================================================
# Copy this file to .env.local and fill in your values

# -----------------------------------------------------------------------------
# Authentication
# -----------------------------------------------------------------------------
# Password for accessing the admin panel (/admin)
# Use a strong, unique password
ADMIN_PASSWORD=your-secure-password-here

# -----------------------------------------------------------------------------
# Site Configuration
# -----------------------------------------------------------------------------
# Public URL of your deployed site (no trailing slash)
# Used for sitemap generation and OAuth redirect URLs
NEXT_PUBLIC_SITE_URL=https://photos.example.com

# -----------------------------------------------------------------------------
# Sync Webhook (Optional)
# -----------------------------------------------------------------------------
# Secret token for triggering photo sync via webhook
# POST to /api/sync/webhook with Authorization: Bearer <secret>
SYNC_WEBHOOK_SECRET=your-webhook-secret-here

# -----------------------------------------------------------------------------
# Adobe Lightroom Integration (Optional)
# -----------------------------------------------------------------------------
# Required only if you want to sync photo titles/captions from Lightroom
#
# To get these credentials:
# 1. Go to https://developer.adobe.com/console
# 2. Create a new project
# 3. Add the "Lightroom" API
# 4. Create OAuth credentials (Web)
# 5. Add your callback URL: {NEXT_PUBLIC_SITE_URL}/api/adobe/callback
#
ADOBE_CLIENT_ID=your-adobe-client-id
ADOBE_CLIENT_SECRET=your-adobe-client-secret
